# Build artifacts (not committed per CLAUDE.md)
emacs-functions.json
functions-compact.jsonl
functions-core.jsonl

# Sentinel files (local pipeline state)
.sentinels/

# Legacy artifacts (kept to prevent accidental commits)
__pycache__/
*.pyc
.venv/
*.egg-info/
uv.lock

# README.md is derived from README.org via gmake sync
README.md

# Environment
.env
